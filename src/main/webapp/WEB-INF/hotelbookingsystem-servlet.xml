<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
    	http://www.springframework.org/schema/beans/spring-beans.xsd
    	http://www.springframework.org/schema/context
    	http://www.springframework.org/schema/context/spring-context.xsd
    	http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

	<!-- Step 3: Add support for component scanning -->
	<context:component-scan
		base-package="com.entlogics.hotelbookingsystem" />

	<!-- Context for properties file -->
	<context:property-placeholder
		location="classpath:db.properties" />

	<!-- Step 4: Add support for conversion, formatting and validation support -->
	<mvc:annotation-driven />
	
	<!-- resources mapping -->
	<mvc:resources location="/resources/" mapping="/resources/**"/>

	<!-- Step 5: Define Spring MVC view resolver -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/view/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<!-- Defining beans for HotelDAOImpl -->
	<bean id="hotelDAO"
		class="com.entlogics.hotelbookingsystem.dao.HotelDAOImpl">
		<!-- Setter injection for dataSource -->
		<property name="dataSource" ref="dataSource" />
	</bean>

	<!-- Defining beans for DataSource -->
	<bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<!-- Setter injection for dataSource -->
		<property name="driverClassName"
			value="com.mysql.cj.jdbc.Driver" />
		<property name="url"
			value="jdbc:mysql://localhost:3306/hoteldb" />
		<property name="username" value="hoteldbuser" />
		<property name="password" value="tanvi" />
	</bean>

</beans>